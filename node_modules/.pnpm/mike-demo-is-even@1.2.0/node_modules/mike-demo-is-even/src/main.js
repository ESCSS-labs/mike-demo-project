import { createESTest } from "escss-estest";
import { isPositive } from "mike-demo-is-positive";

function ESTest(input, type, message = "[demo-is-even] isEven() is GONE. Say hi to isTwo() 🎉") {
 return createESTest(input, type, message)
}

export function isEven(input) {
  {
    ESTest(input, 'null')
  }

  return isPositive(input)
}